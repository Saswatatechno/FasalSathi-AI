/**
 * LoadingPage — full-screen loading/splash page shown during app initialisation.
 *
 * Architecture: App → LoadingPage → LoadingScreen (component)
 *
 * Placeholder. UI implementation will be added in the UI build step.
 */
import React from 'react';

export default function LoadingPage() {
  return null; // placeholder
}
