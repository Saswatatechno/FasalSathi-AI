/**
 * LoadingScreen — displayed while the app initialises or during async operations.
 *
 * Placeholder. UI implementation will be added in the UI build step.
 */
import React from 'react';

export default function LoadingScreen() {
  return null; // placeholder
}
