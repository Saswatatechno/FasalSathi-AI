/**
 * PageHeader — reusable page title and subtitle banner shared across all feature pages.
 *
 * Props (planned):
 *   title    {string} - Page heading
 *   subtitle {string} - Supporting description
 *   icon     {ReactNode} - Optional leading icon
 *
 * Placeholder. UI implementation will be added in the UI build step.
 */
import React from 'react';

export default function PageHeader({ title, subtitle, icon }) {
  return null; // placeholder
}
