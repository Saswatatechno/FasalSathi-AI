/**
 * Button — shared design-system button component.
 *
 * Props (planned):
 *   children  {ReactNode}
 *   variant   {'primary' | 'secondary' | 'danger'}
 *   disabled  {boolean}
 *   loading   {boolean}
 *   onClick   {function}
 *
 * Placeholder. UI implementation will be added in the UI build step.
 */
import React from 'react';

export default function Button({ children, variant = 'primary', disabled = false, loading = false, onClick }) {
  return null; // placeholder
}
