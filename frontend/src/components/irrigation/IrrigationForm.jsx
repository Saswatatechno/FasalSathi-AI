/**
 * IrrigationForm — input form for crop, soil and weather parameters.
 *
 * Architecture: IrrigationPage → IrrigationForm → useIrrigation → irrigationService → POST /api/irrigation/recommend
 *
 * Inputs (planned): crop, soil_moisture, temperature, humidity, rainfall_forecast
 *
 * Placeholder. UI implementation will be added in the UI build step.
 */
import React from 'react';

export default function IrrigationForm({ onSubmit, isLoading }) {
  return null; // placeholder
}
