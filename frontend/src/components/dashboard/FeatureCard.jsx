/**
 * FeatureCard — displays one of the four core feature tiles on the Dashboard.
 *
 * Props (planned):
 *   title       {string}
 *   description {string}
 *   icon        {ReactNode}
 *   onClick     {function}
 *
 * Placeholder. UI implementation will be added in the UI build step.
 */
import React from 'react';

export default function FeatureCard({ title, description, icon, onClick }) {
  return null; // placeholder
}
