/**
 * QuickActions — shortcut action buttons shown on the Dashboard.
 *
 * Placeholder. UI implementation will be added in the UI build step.
 */
import React from 'react';

export default function QuickActions({ setActiveTab }) {
  return null; // placeholder
}
