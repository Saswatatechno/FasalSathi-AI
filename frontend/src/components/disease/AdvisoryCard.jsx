/**
 * AdvisoryCard — displays the actionable advisory text returned by the disease prediction.
 *
 * Props (planned):
 *   advice       {string} - Recommended treatment advisory
 *   observations {string} - Visual symptoms noted by the model
 *   confidence   {string} - Qualitative confidence: 'High' | 'Medium' | 'Low'
 *
 * Placeholder. UI implementation will be added in the UI build step.
 */
import React from 'react';

export default function AdvisoryCard({ advice, observations, confidence }) {
  return null; // placeholder
}
