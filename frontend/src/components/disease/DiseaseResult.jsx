/**
 * DiseaseResult — renders the structured prediction result from the backend.
 *
 * Props (planned):
 *   result {object} - DiseasePredictionResponse shape:
 *     { crop, disease, confidence, observations, advice }
 *
 * Placeholder. UI implementation will be added in the UI build step.
 */
import React from 'react';

export default function DiseaseResult({ result }) {
  return null; // placeholder
}
