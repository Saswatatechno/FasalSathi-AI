/**
 * ImagePreview — displays the selected crop/leaf image before submission.
 *
 * Props (planned):
 *   imageUrl {string} - Object URL of the selected file
 *   onClear  {function} - Callback to clear the selection
 *
 * Placeholder. UI implementation will be added in the UI build step.
 */
import React from 'react';

export default function ImagePreview({ imageUrl, onClear }) {
  return null; // placeholder
}
