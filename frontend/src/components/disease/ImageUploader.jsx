/**
 * ImageUploader — drag-and-drop / click-to-upload component for crop/leaf images.
 *
 * Architecture: DiseaseDetectionPage → ImageUploader → useDiseaseDetection → diseaseService → POST /api/disease/predict
 *
 * Props (planned):
 *   onFileSelect {function(File)} - Callback when user selects an image
 *
 * Placeholder. UI implementation will be added in the UI build step.
 */
import React from 'react';

export default function ImageUploader({ onFileSelect }) {
  return null; // placeholder
}
