/**
 * CropRecommendationForm — input form for soil and environmental parameters.
 *
 * Architecture: CropRecommendationPage → CropRecommendationForm → useCropRecommendation → cropService → POST /api/crop/recommend
 *
 * Inputs (planned): N, P, K, pH, temperature, humidity, rainfall
 *
 * Placeholder. UI implementation will be added in the UI build step.
 */
import React from 'react';

export default function CropRecommendationForm({ onSubmit, isLoading }) {
  return null; // placeholder
}
