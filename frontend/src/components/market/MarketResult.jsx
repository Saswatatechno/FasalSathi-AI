/**
 * MarketResult — displays market price data returned by the backend.
 *
 * Props (planned):
 *   data {object} - MarketPriceResponse shape: { crop, price_per_quintal, market, trend, advisory }
 *
 * Placeholder. UI implementation will be added in the UI build step.
 */
import React from 'react';

export default function MarketResult({ data }) {
  return null; // placeholder
}
