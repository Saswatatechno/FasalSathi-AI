/**
 * MarketSearch — search input for crop market price lookups.
 *
 * Architecture: MarketInsightsPage → MarketSearch → useMarket → marketService → GET /api/market/prices
 *
 * Props (planned):
 *   onSearch {function(query)} - Callback with the search term
 *
 * Placeholder. UI implementation will be added in the UI build step.
 */
import React from 'react';

export default function MarketSearch({ onSearch, isLoading }) {
  return null; // placeholder
}
